{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/data/(.*)", "destination": "/api/data?_path=$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
